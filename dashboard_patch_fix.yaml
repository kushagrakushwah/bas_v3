spec:
  template:
    spec:
      containers:
      - name: dashboard
        image: secureforge-dashboard:v2
        imagePullPolicy: IfNotPresent
        env:
        - name: API_URL
          value: "http://bas-engine:8000/api/v1"
        ports:
        - containerPort: 8501
