apiVersion: v1
kind: Service
metadata:
  name: secureforge-dashboard
  namespace: secureforge
spec:
  selector:
    app: secureforge-dashboard
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
  type: NodePort