apiVersion: v1
kind: Service
metadata:
  name: secureforge-dashboard
  namespace: secureforge
spec:
  selector:
    app: secureforge-dashboard
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort